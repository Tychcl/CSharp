<cw:CWWindow x:Class="TychkclLibraryFund.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cw="clr-namespace:CustomWindow.Controls;assembly=CustomWindow.Controls"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:TychkclLibraryFund"
        mc:Ignorable="d"
            Title="Fund" Height="450" Width="400">
    <Window.Resources>
        <ResourceDictionary Source="Style\Styles.xaml"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="57"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions> Grid.Row="0" Grid.RowSpan="7"-->
        <StackPanel Grid.Column="0">
            <RadioButton x:Name="User" fa:Awesome.Content="UserCircleOutline" Style="{StaticResource MainMenuButton}"/>
            <RadioButton x:Name="Library" fa:Awesome.Content="Building" Style="{StaticResource MainMenuButton}"/>
            <RadioButton x:Name="Literature" fa:Awesome.Content="Book" Style="{StaticResource MainMenuButton}"/>
            <RadioButton x:Name="ReFill" fa:Awesome.Content="PlusSquare" Style="{StaticResource MainMenuButton}"/>
        </StackPanel>
        <Border Background="#33585858" Grid.Column="1"  CornerRadius="0 0 15 0">
            <Frame x:Name="frame" NavigationUIVisibility="Hidden" />
        </Border>
    </Grid>
</cw:CWWindow>
